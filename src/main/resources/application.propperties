spring:
  profiles:
    active: test
  security:
    oauth2:
      client:
        client-id: ${daniellucas}
        client-secret: ${daniellucas123}

jwt:
  secret: ${JWT_SECRET:MY-JWT-SECRET}
  duration: ${JWT_DURATION:86400}
